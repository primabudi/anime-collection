import styled from "@emotion/styled";
import { Button } from "@mui/material";

const Wrapper = styled.div``;

const Title = styled.h1`
  text-align: center;
`;

const CollectionButton = styled(Button)`
  margin: 0 auto 16px;
`;

export { Wrapper, Title, CollectionButton };
